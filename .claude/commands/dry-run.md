Preview all emails in contacts.csv without sending anything.

Run this command and show the output:
```bash
cd ~/outreach && python send_pipeline.py --dry-run
```

Do not ask any questions. Run it directly and show me the full output.
