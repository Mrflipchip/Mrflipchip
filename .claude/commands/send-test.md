Send all emails in contacts.csv to julian.coul@gmail.com (test address — not real contacts).

Run this command:
```bash
cd ~/outreach && python send_pipeline.py --auto-confirm --test-address julian.coul@gmail.com
```

Do not ask any questions. Run it directly and show me the full output including sent confirmations.
