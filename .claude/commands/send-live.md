Send emails to real contacts from contacts.csv. This sends to actual bank recipients.

Run this command:
```bash
cd ~/outreach && python send_pipeline.py
```

The pipeline will show a numbered menu. Select option 3 (Live send) and type YES to confirm.
Show me the full output as it runs.
